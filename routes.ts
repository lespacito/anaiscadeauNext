export const publicRoutes = ["/", "/passions"];

export const authRoutes = ["/login", "/error", "/verify-request"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_REDIRECT = "/dashboard";
